# HiFiBerry required kernel modules
# This file ensures required modules are loaded at boot time
# See modules-load.d(5) for more information

# I2C device interface module for sound card detection
i2c-dev
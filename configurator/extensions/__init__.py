"""HiFiBerry extension installer.

Extensions are Debian packages marked ``XB-Hifiberry-Extension: yes`` in a
signed apt repository. The marker is both the discovery mechanism and the
install allowlist.
"""
